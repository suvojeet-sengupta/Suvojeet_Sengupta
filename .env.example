# Public URL of the standalone VPS backend (Suvojeet-Sengupta-Backend).
# Inlined into the client bundle at build time, so it MUST be set before 'next build'.
# Leave empty to use same-origin (legacy Cloudflare API routes).
NEXT_PUBLIC_API_BASE_URL=https://api.suvojeetsengupta.in
